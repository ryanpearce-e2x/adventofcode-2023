import { Day, IDay, Solution } from '../types/Day';

export default class DayX extends Day implements IDay {
    constructor() {
        super(0);
    }

    partOne(): Solution {
        return 0;
    }

    partTwo(): Solution {
        return 0;
    }
}
